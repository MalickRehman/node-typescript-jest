// const production = process.env.NODE_ENV === 'production';
export const config = {
    // apiurl: production ? 'https://backend.maply.io/graphql' : 'http://localhost:3000/graphql'
    apiurl:'http://localhost:4000'
}
